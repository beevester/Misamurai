{% extends 'layouts/homepage.html.twig' %}

{% block body %}
<div>
{{ include('_includes/nav/main.html.twig')}}
</div>
{{ include('main/nominate/create.html.twig')}}
<!-- loads the nomination -->
<div class="container-fluid">
      <div class="row" style="margin: 100px">
            <div class="col-sm-3">Test</div>
            <div class="col-sm-6">
                <div class="row">
                    {% for i in 0..10 %}
                        {{include('main/nominate/userDetails.html.twig')}}
                    {% endfor %}
                </div> 
            </div>
            <div class="col-sm-3">sskddjf</div>
      </div>
</div>
<!-- end of the load for nomination -->


{% endblock %}
